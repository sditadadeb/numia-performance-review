# Render Blueprint - Deploy automático
# https://render.com/docs/blueprint-spec

services:
  # Backend - Java Spring Boot
  - type: web
    name: performance-review-numia-api
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    region: oregon
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: CORS_ORIGINS
        value: https://performance-review-numia.onrender.com

  # Frontend - React/Vite with Nginx
  - type: web
    name: performance-review-numia
    env: docker
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    region: oregon
    plan: free
